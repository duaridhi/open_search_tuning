version: "3.9"

services:
  jupyter:
    image: run-jupyter-image 
    container_name: run-jupyter-image 
    ports:
      - "8888:8888"
    volumes:
      - ./notebooks:/workspace
    command: >
      jupyter lab
      --ip=0.0.0.0
      --port=8888
      --no-browser
      --allow-root
    restart: unless-stopped
