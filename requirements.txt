# FastAPI & Web Framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Qdrant Vector Database
qdrant-client>=1.7.0

# Embeddings & ML
sentence-transformers>=2.2.0
torch>=2.0.0
transformers>=4.30.0
datasets>=2.10.0

# S3/MinIO & Object Storage
boto3>=1.26.0

# Utilities
tqdm>=4.66.0
nltk>=3.8.0
numpy>=1.24.0
httpx>=0.24.0

# HuggingFace Hub + Inference (used by app.py, chat_hf.py, hf_utils.py).
# huggingface_hub >= 1.12 pulls in typer, which needs click >= 8.2 (TyperChoice
# subscripts click.Choice). Pin to avoid the "type 'Choice' is not subscriptable"
# regression when click 8.1.x gets resolved transitively.
huggingface_hub>=0.23.0
click>=8.2

